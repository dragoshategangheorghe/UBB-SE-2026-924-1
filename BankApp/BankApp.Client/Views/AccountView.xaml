<Page
    x:Class="BankApp.Client.Views.AccountView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="using:BankApp.Client.Utilities"
    mc:Ignorable="d">

    <Page.Resources>
        <utils:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <utils:DecimalToCurrencyConverter x:Key="CurrencyConverter" />
        <utils:DateToOpenedLabelConverter x:Key="OpenedDateConverter" />
        <utils:AccountStatusToBrushConverter x:Key="StatusToBrush" />
    </Page.Resources>

    <Grid Background="#F5F7FB">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="28" Spacing="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Accounts" FontSize="30" FontWeight="SemiBold" Foreground="#111827" />
                        <TextBlock Text="View secured balances, account types, and status details for the authenticated session." Foreground="#6B7280" TextWrapping="Wrap" />
                    </StackPanel>

                    <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Style="{ThemeResource AccentButtonStyle}" />
                </Grid>

                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border CornerRadius="18" Background="White" Padding="18">
                        <StackPanel Spacing="6">
                            <TextBlock Text="Accounts" Foreground="#6B7280" />
                            <TextBlock Text="{Binding AccountCount}" FontSize="28" FontWeight="SemiBold" Foreground="#111827" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" CornerRadius="18" Background="White" Padding="18">
                        <StackPanel Spacing="6">
                            <TextBlock Text="Total Balance" Foreground="#6B7280" />
                            <TextBlock Text="{Binding TotalBalance, Converter={StaticResource CurrencyConverter}}" FontSize="28" FontWeight="SemiBold" Foreground="#0F766E" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" CornerRadius="18" Background="White" Padding="18">
                        <StackPanel Spacing="6">
                            <TextBlock Text="Session" Foreground="#6B7280" />
                            <TextBlock Text="Protected" FontSize="28" FontWeight="SemiBold" Foreground="#1D4ED8" />
                        </StackPanel>
                    </Border>
                </Grid>

                <InfoBar IsOpen="{Binding HasError}" Severity="Error" Message="{Binding StatusMessage}" />
                <InfoBar IsOpen="{Binding IsLoading}" Severity="Informational" Message="Loading secured account data..." />

                <Border CornerRadius="20" Background="White" Padding="18">
                    <StackPanel Spacing="14">
                        <TextBlock Text="Account Portfolio" FontSize="18" FontWeight="SemiBold" Foreground="#111827" />

                        <ListView ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount, Mode=TwoWay}" SelectionMode="Single" BorderThickness="0">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="16" BorderThickness="1" BorderBrush="#E5E7EB" Background="#FBFCFE" Padding="16" Margin="0,0,0,12">
                                        <Grid ColumnSpacing="16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Spacing="4">
                                                <TextBlock Text="{Binding AccountName}" FontSize="18" FontWeight="SemiBold" Foreground="#111827" />
                                                <TextBlock Text="{Binding IBAN}" FontFamily="Consolas" Foreground="#6B7280" />
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <Border Background="#EEF2FF" CornerRadius="999" Padding="10,4">
                                                        <TextBlock Text="{Binding AccountType}" Foreground="#4338CA" />
                                                    </Border>
                                                    <Border Background="#F3F4F6" CornerRadius="999" Padding="10,4">
                                                        <TextBlock Text="{Binding Currency}" Foreground="#4B5563" />
                                                    </Border>
                                                    <Border Background="{Binding Status, Converter={StaticResource StatusToBrush}}" CornerRadius="999" Padding="10,4">
                                                        <TextBlock Text="{Binding Status}" Foreground="White" />
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="4">
                                                <TextBlock Text="{Binding Balance, Converter={StaticResource CurrencyConverter}}" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="#0F172A" />
                                                <TextBlock Text="{Binding CreatedAt, Converter={StaticResource OpenedDateConverter}}" HorizontalAlignment="Right" Foreground="#6B7280" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Background="#80F5F7FB" Visibility="{Binding LoadingVisibility}">
            <ProgressRing Width="56" Height="56" IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>